#!/bin/bash

source $HOME/.bash/git

setup-git-gitlab() {
  setup-git GitLab pleitzen@gitlab.com
}

rs() {
  bin/spring rspec "$@"
}

rrs() {
  rs --order "random:$RANDOM" "$@"
}

rs-loop() {
  while :; do
    rs "$@" || break
  done
}

rrs-loop() {
  while :; do
    rrs "$@" || break
  done
}
